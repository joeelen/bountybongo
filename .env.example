# Bountyrunner Environment Configuration

# Optional: PostgreSQL Database connection string
# (If not provided, Bountyrunner automatically runs in In-Memory DB Mode out-of-the-box!)
# You can connect Neon, Supabase, or Vercel Postgres:
# DATABASE_URL=postgresql://user:password@host:5432/bountyrunner?sslmode=require
